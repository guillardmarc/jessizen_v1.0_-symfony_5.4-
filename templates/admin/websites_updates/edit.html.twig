{% extends 'base_dashboard.html.twig' %}

{% block title %}/Modification mise à jour {{ websites_update.title }}{% endblock %}

{% block header %}
    <header class="bg-glass">
        <div class="container">
            <div class="vh-20 d-flex flex-column flex-lg-row align-items-center justify-content-evenly">
                <h1 class="display-1">
                    Mise à jour {{ websites_update.title }}
                </h1>
                <div class="btn-group">
                    <a href="{{ path('app_websites_updates_index') }}" class="btn btn-outline-secondary-custom" title="Retour vers la liste des articles">
                        <i class="bi bi-list-ul"></i>
                    </a>
                    {{ include('admin/websites_updates/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </header>
{% endblock %}

{% block body %}
    <section class="my-5 p-3 bg-glass">
        {{ include('admin/websites_updates/_form.html.twig', {'button_label': 'Modifier'}) }}
    </section>
{% endblock %}
